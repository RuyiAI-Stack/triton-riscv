diff --git a/CMakeLists.txt b/CMakeLists.txt
index 3f066ea916..714c89cba4 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -134,7 +134,7 @@ execute_process(
 )
 include("${TRITON_THIRD_PARTY_CMAKE_VARS_FILE}")
 
-if(TRITON_BUILD_PYTHON_MODULE)
+if(TRITON_BUILD_PYTHON_MODULE AND "nvidia" IN_LIST TRITON_CODEGEN_BACKENDS)
   set_property(
     DIRECTORY
     APPEND
@@ -295,6 +295,17 @@ include_directories(${PROJECT_BINARY_DIR}/include) # Tablegen'd files
 include_directories(${PROJECT_SOURCE_DIR}/third_party)
 include_directories(${PROJECT_BINARY_DIR}/third_party) # Tablegen'd files
 
+# NVWSIR/NVWSTransforms have no external NVIDIA deps but are referenced
+# unconditionally by core TritonGPUTransforms. When the nvidia backend is active
+# the full third_party/nvidia/ tree provides them. When building CPU-only we
+# still need these dialect IRs and their tablegen outputs.
+if (NOT "nvidia" IN_LIST TRITON_CODEGEN_BACKENDS)
+  include_directories(${PROJECT_SOURCE_DIR}/third_party/nvidia/include)
+  include_directories(${PROJECT_BINARY_DIR}/third_party/nvidia/include)
+  add_subdirectory(third_party/nvidia/include/Dialect/NVWS)
+  add_subdirectory(third_party/nvidia/lib/Dialect/NVWS)
+endif()
+
 add_subdirectory(include)
 add_subdirectory(lib)
 
@@ -325,8 +336,9 @@ if(TRITON_BUILD_PYTHON_MODULE)
   if (TRITON_BUILD_PROTON)
     add_subdirectory(third_party/proton)
   endif()
-  # We always build proton dialect
-  list(APPEND TRITON_PLUGIN_NAMES "proton")
+  if (TRITON_CODEGEN_BACKENDS)
+    list(APPEND TRITON_PLUGIN_NAMES "proton")
+  endif()
   add_subdirectory(third_party/proton/Dialect)
 
   if (DEFINED TRITON_PLUGIN_DIRS)
@@ -373,13 +387,32 @@ if(TRITON_BUILD_PYTHON_MODULE)
 
     # LLVM
     LLVMPasses
-    LLVMNVPTXCodeGen
-    LLVMAMDGPUCodeGen
-    LLVMAMDGPUAsmParser
 
     Python3::Module
     pybind11::headers
   )
+  # Conditionally link NVIDIA LLVM codegen only when NVPTX target is available.
+  # Skipped when building without NV backend (e.g. CPU-only / triton-shared builds).
+  if ("NVPTX" IN_LIST LLVM_TARGETS_TO_BUILD)
+    list(APPEND TRITON_LIBRARIES LLVMNVPTXCodeGen)
+  endif()
+
+  # Conditionally link AMD LLVM codegen only when AMDGPU target is available.
+  if ("AMDGPU" IN_LIST LLVM_TARGETS_TO_BUILD)
+    list(APPEND TRITON_LIBRARIES LLVMAMDGPUCodeGen LLVMAMDGPUAsmParser)
+  endif()
+
+  # Conditionally link RISCV LLVM codegen when the RISCV target is available
+  # (e.g. when using a buddy-mlir LLVM built with LLVM_TARGETS_TO_BUILD=host;RISCV).
+  if ("RISCV" IN_LIST LLVM_TARGETS_TO_BUILD)
+    list(APPEND TRITON_LIBRARIES
+        LLVMRISCVCodeGen
+        LLVMRISCVAsmParser
+        LLVMRISCVDesc
+        LLVMRISCVDisassembler
+        LLVMRISCVInfo
+    )
+  endif()
   if(CMAKE_SYSTEM_PROCESSOR MATCHES "aarch64" OR # Linux arm64
      CMAKE_SYSTEM_PROCESSOR MATCHES "arm64" OR # macOS arm64
      CMAKE_OSX_ARCHITECTURES MATCHES "arm64")  # also macOS arm64
@@ -397,6 +430,11 @@ if(TRITON_BUILD_PYTHON_MODULE)
         LLVMPowerPCAsmParser
         LLVMPowerPCCodeGen
       )
+  elseif(CMAKE_SYSTEM_PROCESSOR MATCHES "riscv64")
+      list(APPEND TRITON_LIBRARIES
+        LLVMRISCVCodeGen
+        LLVMRISCVAsmParser
+      )
   else()
     message(FATAL_ERROR "LLVM codegen/ASM parser libs: This HW architecture (${CMAKE_SYSTEM_PROCESSOR}) is not configured in cmake lib dependencies.")
   endif()
@@ -412,6 +450,11 @@ if(TRITON_BUILD_PYTHON_MODULE)
 
   set(TRITON_BACKENDS_TUPLE "(${TRITON_BACKENDS_TUPLE})")
   add_compile_definitions(TRITON_BACKENDS_TUPLE=${TRITON_BACKENDS_TUPLE})
+
+  if ("amd" IN_LIST TRITON_CODEGEN_BACKENDS)
+    add_compile_definitions(TRITON_ENABLE_AMD)
+  endif()
+
   add_library(triton SHARED ${PYTHON_SRC_PATH}/main.cc
                   ${PYTHON_SRC_PATH}/ir.cc
                   ${PYTHON_SRC_PATH}/gluon_ir.cc
@@ -502,8 +551,11 @@ endif()
 find_package(Threads REQUIRED)
 
 add_subdirectory(third_party/f2reduce)
-add_subdirectory(bin)
-add_subdirectory(test)
+# bin/ and test/ depend on NV/AMD-specific tools/headers; skip when no GPU backends are active.
+if (TRITON_CODEGEN_BACKENDS)
+  add_subdirectory(bin)
+  add_subdirectory(test)
+endif()
 
 if(TRITON_BUILD_UT)
   add_subdirectory(unittest)
diff --git a/lib/Conversion/CMakeLists.txt b/lib/Conversion/CMakeLists.txt
index 71007231e3..73bb960554 100644
--- a/lib/Conversion/CMakeLists.txt
+++ b/lib/Conversion/CMakeLists.txt
@@ -1,3 +1,2 @@
 add_subdirectory(TritonToTritonGPU)
 add_subdirectory(TritonGPUToLLVM)
-add_subdirectory(TritonInstrumentToLLVM)
diff --git a/python/src/gluon_ir.cc b/python/src/gluon_ir.cc
index 7900a42734..5e9b79e1e8 100644
--- a/python/src/gluon_ir.cc
+++ b/python/src/gluon_ir.cc
@@ -9,9 +9,11 @@
 #include "mlir/IR/BuiltinTypes.h"
 #include "mlir/IR/DialectRegistry.h"
 #include "mlir/IR/Types.h"
+#ifdef TRITON_ENABLE_AMD
 #include "third_party/amd/include/Dialect/TritonAMDGPU/IR/Dialect.h"
 #include "third_party/amd/lib/TritonAMDGPUToLLVM/TargetInfo.h"
 #include "third_party/amd/lib/TritonAMDGPUTransforms/Utility.h"
+#endif
 #include "triton/Analysis/Utility.h"
 #include "triton/Dialect/Gluon/IR/Dialect.h"
 #include "triton/Dialect/Triton/IR/Dialect.h"
@@ -34,7 +36,9 @@ namespace tt = triton;
 namespace ttg = triton::gpu;
 namespace ttng = triton::nvidia_gpu;
 namespace gluon = mlir::triton::gluon;
+#ifdef TRITON_ENABLE_AMD
 namespace ttag = mlir::triton::amdgpu;
+#endif
 
 namespace {
 
@@ -138,16 +142,22 @@ struct GluonLayouts {
   py::handle NVMMASharedLayout;
   py::handle SwizzledSharedLayout;
   py::handle SharedLinearLayout;
+#ifdef TRITON_ENABLE_AMD
   py::handle AMDMFMALayout;
   py::handle AMDWMMALayout;
+#endif
   py::handle PaddedSharedLayout;
+#ifdef TRITON_ENABLE_AMD
   py::handle PartitionedSharedLayout;
+#endif
 
   GluonLayouts() {
     auto layouts =
         py::module::import("triton.experimental.gluon.language._layouts");
+#ifdef TRITON_ENABLE_AMD
     auto amdLayouts =
         py::module::import("triton.experimental.gluon.language.amd._layouts");
+#endif
     auto blackwellLayouts = py::module::import(
         "triton.experimental.gluon.language.nvidia.blackwell");
     AutoLayout = py::object(layouts.attr("AutoLayout")).release();
@@ -168,14 +178,18 @@ struct GluonLayouts {
         py::object(layouts.attr("SwizzledSharedLayout")).release();
     SharedLinearLayout =
         py::object(layouts.attr("SharedLinearLayout")).release();
+#ifdef TRITON_ENABLE_AMD
     AMDMFMALayout = py::object(amdLayouts.attr("AMDMFMALayout")).release();
     AMDWMMALayout = py::object(amdLayouts.attr("AMDWMMALayout")).release();
+#endif
     PaddedSharedLayout =
         py::object(layouts.attr("PaddedSharedLayout")).release();
+#ifdef TRITON_ENABLE_AMD
     auto gfx1250Layouts = py::module::import(
         "triton.experimental.gluon.language.amd.gfx1250._layouts");
     PartitionedSharedLayout =
         py::object(gfx1250Layouts.attr("PartitionedSharedLayout")).release();
+#endif
 
     auto core = py::module::import("triton.language.core");
   }
@@ -258,6 +272,7 @@ py::object layoutToGluon(Attribute layout) {
     return layouts.AutoLayout();
   } else if (auto autoEnc = dyn_cast<gluon::CoalescedEncodingAttr>(layout)) {
     return layouts.CoalescedLayout();
+#ifdef TRITON_ENABLE_AMD
   } else if (auto amdMfma = dyn_cast<ttg::AMDMfmaEncodingAttr>(layout)) {
     auto cgaBases = getCgaLayoutBases(amdMfma.getCGALayout());
     return layouts.AMDMFMALayout(
@@ -275,6 +290,7 @@ py::object layoutToGluon(Attribute layout) {
         amdWmma.getVersion(), amdWmma.getIsTransposed(),
         ctaLayout.getBases().lookup(kWarp), ctaLayout.getBases().lookup(kReg),
         toStdVector(amdWmma.getInstrShape()), cgaBases, amdWmma.getRank());
+#endif
   } else if (auto paddedShared =
                  dyn_cast<ttg::PaddedSharedEncodingAttr>(layout)) {
     auto *ctx = paddedShared.getContext();
@@ -298,11 +314,15 @@ py::object layoutToGluon(Attribute layout) {
                                       shape);
   } else if (auto partitioned =
                  dyn_cast<ttg::PartitionedSharedEncodingAttr>(layout)) {
+#ifdef TRITON_ENABLE_AMD
     py::object partitionLayout =
         layoutToGluon(partitioned.getPartitionLayout());
     return layouts.PartitionedSharedLayout(
         partitioned.getNumPartitions(), partitioned.getNumGroups(),
         partitioned.getPartitionDim(), partitionLayout);
+#else
+    (void)partitioned;
+#endif
   } else if (auto tmemScales =
                  dyn_cast<ttng::TensorMemoryScalesEncodingAttr>(layout)) {
     return layouts.TensorMemoryScalesLayout(
@@ -473,6 +493,7 @@ void init_gluon_ir(py::module &&m) {
                  ctx, version[0], version[1], warpsPerCta, cgaLayout,
                  instrShape);
            })
+#ifdef TRITON_ENABLE_AMD
       .def("get_amd_mfma_layout",
            [](GluonOpBuilder &self, unsigned version,
               std::vector<unsigned> &warpsPerCta,
@@ -503,6 +524,7 @@ void init_gluon_ir(py::module &&m) {
              return ttg::AMDWmmaEncodingAttr::get(
                  ctx, version, ctaLayout, transposed, cgaLayout, instrShape);
            })
+#endif
       .def("get_padded_shared_layout",
            [](GluonOpBuilder &self, std::vector<unsigned> &intervals,
               std::vector<unsigned> &paddings,
@@ -661,6 +683,7 @@ void init_gluon_ir(py::module &&m) {
                  pointer, smem, mask, other, cacheModifier, evictionPolicy,
                  isVolatile);
            })
+#ifdef TRITON_ENABLE_AMD
       .def("create_async_copy_local_to_global",
            [](GluonOpBuilder &self, Value smem, Value pointer, Value mask,
               tt::CacheModifier cacheModifier,
@@ -668,6 +691,7 @@ void init_gluon_ir(py::module &&m) {
              self.create<ttag::AsyncCopyLocalToGlobalOp>(
                  smem, pointer, mask, cacheModifier, evictionPolicy);
            })
+#endif
       .def("create_async_copy_mbarrier_arrive",
            [](GluonOpBuilder &self, Value mbarrier, bool incrementCount) {
              self.create<ttng::AsyncCopyMbarrierArriveOp>(mbarrier,
@@ -739,6 +763,7 @@ void init_gluon_ir(py::module &&m) {
               int bitwidth) -> int {
              auto regLayout = ttg::toLinearLayout(shape, regLayoutAttr);
              auto smemLayout = ttg::toLinearLayout(shape, sharedLayoutAttr);
+#ifdef TRITON_ENABLE_AMD
              auto mod = self.getBuilder()
                             .getInsertionBlock()
                             ->getParentOp()
@@ -761,6 +786,9 @@ void init_gluon_ir(py::module &&m) {
                     "srcTile.laneAddr should be empty");
              return ttg::bankConflictsMemDesc(regLayout, smemLayout, bitwidth,
                                               numBanks, dstTile);
+#else
+             return ttg::bankConflictsMemDesc(regLayout, smemLayout, bitwidth);
+#endif
            })
       .def("create_local_dealloc",
            [](GluonOpBuilder &self, Value memDesc) -> Operation * {
@@ -1041,6 +1069,7 @@ void init_gluon_ir(py::module &&m) {
              return self.create<ttg::WarpSpecializeOp>(resultTypes,
                                                        partitionNumWarps);
            })
+#ifdef TRITON_ENABLE_AMD
       .def("create_buffer_load",
            [](GluonOpBuilder &self, Type resultType, Value ptr, Value offsets,
               Value mask, Value other, tt::CacheModifier cache) -> Value {
@@ -1184,7 +1213,9 @@ void init_gluon_ir(py::module &&m) {
                border->setAttr("triton.warp_pipeline.priority",
                                IntegerAttr::get(i32Ty, priority));
              }
-           });
+           })
+#endif
+      ;
 
   m.def(
       "compute_tmem_reg_layout",
@@ -1253,6 +1284,7 @@ void init_gluon_ir(py::module &&m) {
         return getCgaLayoutBases(attr);
       });
 
+#ifdef TRITON_ENABLE_AMD
   m.def("get_amd_mfma_scale_layout",
         [](unsigned opIdx, std::vector<int64_t> &shape, unsigned mfmaMDim,
            std::vector<unsigned> &tilesPerWarp,
@@ -1344,6 +1376,7 @@ void init_gluon_ir(py::module &&m) {
           auto attr = ttg::LinearEncodingAttr::get(&ctx, ll);
           return layoutToGluon(attr);
         });
+#endif
 
   m.def("get_layout_view",
         [](py::object layout, std::vector<int64_t> shape,
diff --git a/setup.py b/setup.py
index 0e6c0eac7f..321a60b0a3 100644
--- a/setup.py
+++ b/setup.py
@@ -293,6 +293,10 @@ class CMakeBuild(build_ext):
             "-DTRITON_WHEEL_DIR=" + wheeldir,
             f"-DTRITON_CACHE_PATH={get_triton_cache_path()}",
             f"-DTRITON_VERSION={TRITON_VERSION}",
+            # Unit tests and Proton profiler require CUPTI/ROCtracer headers that
+            # are only present when a GPU backend is active.
+            "-DTRITON_BUILD_UT=" + ("ON" if any(not b.is_external for b in backends) else "OFF"),
+            "-DTRITON_BUILD_PROTON=" + ("ON" if any(not b.is_external for b in backends) else "OFF"),
         ]
         if lit_dir is not None:
             cmake_args.append("-DLLVM_EXTERNAL_LIT=" + lit_dir)
@@ -361,7 +365,7 @@ class CMakeBuild(build_ext):
         ]
         cmake_args += [f"-D{option}={os.getenv(option)}" for option in passthrough_args if option in os.environ]
 
-        if check_env_flag("TRITON_BUILD_PROTON", "ON"):  # Default ON
+        if check_env_flag("TRITON_BUILD_PROTON", "ON") and any(not b.is_external for b in backends):
             cmake_args += self.get_proton_cmake_args()
 
         if is_offline_build():
@@ -380,7 +384,7 @@ class CMakeBuild(build_ext):
         subprocess.check_call(["cmake", "--build", ".", "--target", "mlir-doc"], cwd=cmake_dir)
 
 
-backends = [*BackendInstaller.copy(["nvidia", "amd"]), *BackendInstaller.copy_externals()]
+backends = [*BackendInstaller.copy_externals()]
 
 
 def get_package_dirs():
diff --git a/third_party/proton/Dialect/CMakeLists.txt b/third_party/proton/Dialect/CMakeLists.txt
index 9ef6bdc31a..3f9bca4a0e 100644
--- a/third_party/proton/Dialect/CMakeLists.txt
+++ b/third_party/proton/Dialect/CMakeLists.txt
@@ -2,7 +2,7 @@ include_directories(${CMAKE_CURRENT_SOURCE_DIR}/include)
 include_directories(${CMAKE_CURRENT_BINARY_DIR}/include)
 add_subdirectory(include)
 add_subdirectory(lib)
-if(TRITON_BUILD_PYTHON_MODULE)
-  add_triton_plugin(TritonProton ${CMAKE_CURRENT_SOURCE_DIR}/triton_proton.cc LINK_LIBS ProtonToProtonGPU ProtonGPUToLLVM ProtonAMDGPUToLLVM ProtonNVIDIAGPUToLLVM ProtonAnalysis)
+if(TRITON_BUILD_PYTHON_MODULE AND TRITON_CODEGEN_BACKENDS)
+  add_triton_plugin(TritonProton ${CMAKE_CURRENT_SOURCE_DIR}/triton_proton.cc LINK_LIBS ProtonToProtonGPU ProtonGPUToLLVM ProtonAMDGPUToLLVM ProtonNVIDIAGPUToLLVM ProtonAnalysis)
   target_link_libraries(TritonProton PRIVATE Python3::Module pybind11::headers)
 endif()
diff --git a/third_party/proton/Dialect/lib/ProtonGPUToLLVM/CMakeLists.txt b/third_party/proton/Dialect/lib/ProtonGPUToLLVM/CMakeLists.txt
index 3a568c218c..adc5f4c91d 100644
--- a/third_party/proton/Dialect/lib/ProtonGPUToLLVM/CMakeLists.txt
+++ b/third_party/proton/Dialect/lib/ProtonGPUToLLVM/CMakeLists.txt
@@ -12,5 +12,9 @@ add_triton_library(ProtonGPUToLLVM
     ProtonAnalysis
 )
 
-add_subdirectory(ProtonNvidiaGPUToLLVM)
-add_subdirectory(ProtonAMDGPUToLLVM)
+if ("nvidia" IN_LIST TRITON_CODEGEN_BACKENDS)
+  add_subdirectory(ProtonNvidiaGPUToLLVM)
+endif()
+if ("amd" IN_LIST TRITON_CODEGEN_BACKENDS)
+  add_subdirectory(ProtonAMDGPUToLLVM)
+endif()
diff --git a/python/triton/experimental/gluon/__init__.py b/python/triton/experimental/gluon/__init__.py
index c54c7bef92..d2ee693ca9 100644
--- a/python/triton/experimental/gluon/__init__.py
+++ b/python/triton/experimental/gluon/__init__.py
@@ -1,6 +1,4 @@
 from ._runtime import GluonJITFunction, constexpr_function, jit
 from triton import must_use_result, aggregate
-from . import nvidia
-from . import amd
 
-__all__ = ["aggregate", "amd", "constexpr_function", "GluonJITFunction", "jit", "must_use_result", "nvidia"]
+__all__ = ["aggregate", "constexpr_function", "GluonJITFunction", "jit", "must_use_result"]
diff --git a/python/triton/experimental/gluon/amd/gfx1250.py b/python/triton/experimental/gluon/amd/gfx1250.py
index 9e79c5231e..35dc7be3ee 100644
--- a/python/triton/experimental/gluon/amd/gfx1250.py
+++ b/python/triton/experimental/gluon/amd/gfx1250.py
@@ -2,7 +2,10 @@ from dataclasses import dataclass
 from typing import List, Any
 from triton._utils import validate_block_shape
 from triton.experimental.gluon.language._layouts import PaddedSharedLayout, SwizzledSharedLayout
-from triton.experimental.gluon.language.amd.gfx1250 import PartitionedSharedLayout
+try:
+    from triton.experimental.gluon.language.amd.gfx1250 import PartitionedSharedLayout
+except ImportError:
+    PartitionedSharedLayout = type(None)
 
 __all__ = ["TensorDescriptor"]
 
diff --git a/python/triton/experimental/gluon/language/__init__.py b/python/triton/experimental/gluon/language/__init__.py
index 42db164ee9..dc8f2aa1d4 100644
--- a/python/triton/experimental/gluon/language/__init__.py
+++ b/python/triton/experimental/gluon/language/__init__.py
@@ -135,6 +135,4 @@ from ._standard import (
     zeros_like,
 )
 
-from . import nvidia
-from . import amd
 from . import extra
